{$DOMAIN:localhost} {
    encode zstd gzip
    reverse_proxy latticescholar:8765
    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains"
        -Server
    }
    log {
        output stdout
        format console
    }
}
