# Contributing to LatticeScholar

感谢你帮助高校科研人员获得更可信、更轻量的工具。贡献前请先搜索已有 Issue；较大的产品或数据源改动建议先开 Discussion 说明用户问题、证据来源、维护成本和隐私影响。

## 开发环境

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
ruff check .
pytest -q
```

## 贡献要求

- 新增学术数据源必须使用官方 API/数据下载方式，记录许可、额度与失败策略；
- 新增分数必须可解释，禁止用分数暗示无法验证的质量或成功概率；
- 新增 AI 能力必须有无模型回退、结构校验、输入上限与误用提醒；
- 新增政策必须来自官方页面，并记录发布方、日期、原文链接和人工核验范围；
- 自动发现的政策候选不得跳过 `pending → approved/rejected` 审核流程；政策源变化需附解析测试；
- Bug 修复应带回归测试；界面改动应兼顾键盘操作、移动端和空状态；
- 不提交 API Key、个人数据、受限论文全文或未获授权的训练/评测材料。

## Commit 与 PR

建议使用小而清晰的提交：`feat:`, `fix:`, `docs:`, `test:`, `refactor:`。PR 描述应包含用户问题、改动、验证方式、截图（界面变更时）和风险/回滚说明。

仓库内置 Bug、功能建议和政策源三类 Issue Form。涉及多个工作流的提议请先说明具体用户、发生频率、现有替代方法、可核验结果和长期维护成本；“加入某个 AI 功能”本身不是完整需求。

提交贡献即表示你有权按项目的 Apache-2.0 许可证提供该内容。涉及计费、账号、权限、验证码、Webhook 或数据隔离的改动必须附带安全测试；不要在 PR 中放入生产邮箱、支付密钥或真实用户记录。
