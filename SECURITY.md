# Security Policy

## Supported versions

安全更新优先覆盖最新发布版本和 `main` 分支。

## Reporting a vulnerability

请不要公开披露仍可被利用的漏洞。通过项目所有者指定的私有渠道报告影响版本、复现步骤、预期影响和建议修复；不要在公开 Issue 或论坛发布漏洞细节。

不要在报告中附带真实 API Key、支付密钥、验证码、个人数据、未公开论文或受限数据。公网部署必须使用邮箱账号模式与 TLS，并配置边缘限流、SMTP 安全、签名 Webhook、备份和日志治理。管理员赠权和订阅状态相关漏洞应按高优先级私下报告。

模型控制台的网页 BYOK 密钥以加密密文保存；生产环境必须通过密钥管理器设置稳定的 `LATTICE_CREDENTIAL_ENCRYPTION_KEY`。自定义模型地址默认只允许本机，任意远程主机需要显式启用并应配合出站网络白名单。疑似明文密钥回显、跨账号读取、SSRF、路由越权或日志泄露均按高优先级漏洞处理。
